# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/synccode?retryWrites=true&w=majority

# Google Gemini API key for AI features (required)
API_KEY=your_google_api_key_here

# Judge0 code execution (optional - uses public ce.judge0.com by default)
# JUDGE0_API_URL=https://judge0-ce.p.rapidapi.com
# JUDGE0_API_KEY=your_rapidapi_key_here

# Port (set automatically by Render, defaults to 5000 locally)
# PORT=5000

# Client backend URL (optional - auto-detected in production)
# REACT_APP_BACKEND_URL=http://localhost:5000

# AI model fallback chain, tried left to right (optional).
# Use DIFFERENT models, not snapshots of one: the Gemini free tier meters quota
# per model per day, so falling back to the same model buys nothing.
# AI_MODELS=gemini-3.6-flash,gemini-2.5-flash,gemini-3.5-flash,gemini-2.5-flash-lite,gemini-3.1-flash-lite,gemini-flash-latest

# Embedding model used for RAG (optional). Must be a model your key can access —
# `text-embedding-004` has been retired and 404s.
# EMBEDDING_MODEL=gemini-embedding-001
# EMBEDDING_DIMENSIONS=768
